[project]
name = "smolaton"
version = "0.1.0"
description = "Backend for smolaton"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.117.1",
    "psycopg2>=2.9.10",
]
